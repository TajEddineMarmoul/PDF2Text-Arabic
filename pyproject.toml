[project]
name = "pdf2text-arabic"
version = "0.1.1"
description = "Arabic PDF text extraction with ligature and RTL fixes"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["pymupdf>=1.27.2.2", "tqdm>=4.67.3"]

[project.scripts]
pdf2text-arabic = "pdf2text_arabic.cli:main"
